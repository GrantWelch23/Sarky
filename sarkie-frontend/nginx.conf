server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Support Vue Router HTML5 history mode
    location / {
        try_files $uri $uri/ /index.html;
    }
}
